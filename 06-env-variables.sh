#!/bin/bash

# export COURSE="devsecops with AWS" we can declare it outside the script
# we can declare in .bashrc file in home directory of user
# we can access the variable in the acript without declaring the in the script


echo " course name is : $COURSE "